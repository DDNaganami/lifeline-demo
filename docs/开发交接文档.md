# LifeLine 人生战略曲线 · 开发交接文档

> 交接对象：接手这个项目的开发同学
> 当前状态：**第一阶段纯前端可点击原型**，已跑通「回看 — 核对 — 追问 — 沉淀」完整主路径。
> 所有曲线与文案均为模拟数据；**没有**接排盘计算、**没有**接 AI、**没有**登录/数据库/付费。

---

## 1. 五分钟跑起来

```bash
npm install
npm run build     # 改动代码后需要重新构建
npm start         # 默认 http://localhost:3000，端口用环境变量 PORT 指定
```

### ⚠️ 必读：开发模式在当前环境不可用

**`npm run dev` 在这台机器上无法正常挂载客户端**：Next.js 开发模式的热更新 WebSocket
（`ws://host/_next/hmr`）握手失败（`net::ERR_INVALID_HTTP_RESPONSE`），
导致 React 无法完成 hydration——页面能打开、但所有点击都没有反应。

已用一个只含 `useState` + `onClick` 的空页面验证过：**同样点不动**。
所以这是运行环境问题，与业务代码无关；`npm run build` + `npm start` 的正式版本完全正常。

**结论：本地查看请用 build + start。** 换一台网络环境干净、或能正常建立本地 WebSocket 的机器，
`npm run dev` 大概率可以直接用。若必须用 dev 模式，排查方向：本地代理/安全软件拦截
`/_next/hmr` 的 WebSocket 升级请求。

---

## 2. 技术栈与版本

| 项 | 版本 | 说明 |
|---|---|---|
| Next.js | 16.3.5 | App Router；**Turbopack 为默认打包器** |
| React | 19.2.8 | 仅用 `useState` / `useMemo` / `useRef` / `useEffect` |
| TypeScript | ^5 | `strict: true` |
| Tailwind CSS | ^4 | CSS-first 配置（无 `tailwind.config.js`），见 `app/globals.css` |
| 曲线 | 手写 SVG | **未使用 Recharts**，见 §7.3 |

### 关于 shadcn/ui

原始需求里提到 shadcn/ui。**实际未引入**，原因：原型只有 5 类基础控件（按钮、输入框、
下拉、标签页、折叠区），全部用 Tailwind 手写约 300 行即可，引入 shadcn 会增加
CLI 依赖、`components/ui/*` 生成物与 Radix 依赖，对当前阶段是净负担。

如果后续要引入，建议时机是**开始做多主题/无障碍补全/复杂浮层**时；
届时 `components/*.tsx` 中的按钮与卡片可以直接替换为 shadcn 组件，Props 无需改动。

### Next.js 16 注意事项（与 14/15 写法不同）

- `params` / `searchParams` / `cookies()` / `headers()` **只能异步访问**（原型未用到）。
- `next lint` 已移除，改用 `npx eslint .`；`next build` 不再跑 lint。
- 生成类型助手：`LayoutProps<'/'> `、`PageProps<'/dashboard'>` 由 `next typegen` 生成。
  当前 `app/layout.tsx` 用的是普通 `{ children: ReactNode }`，避免依赖生成产物。
- ESLint 默认 flat config，并启用 `react-hooks` 新规则
  （其中 `set-state-in-effect` 会拦截"在 effect 里同步 setState"）。

---

## 3. 目录结构

```
app/
  layout.tsx                根布局（lang="zh-CN"，viewport，metadata）
  page.tsx                  首页 /
  globals.css               Tailwind 入口 + 设计令牌 + 两处自定义动画/滚动条
  dashboard/page.tsx        /dashboard（仅渲染客户端组件，metadata 在此）
components/
  birth-form.tsx            出生信息表单（含时辰选项常量）
  dashboard-client.tsx      ★ 仪表盘容器：读取本地数据 + 全部状态与编排（493 行）
  life-curve.tsx            ★ 纯 SVG 人生曲线（含自动滚动到"现在"）
  dimension-tabs.tsx        六维度切换
  year-card.tsx             年度卡片
  feedback-buttons.tsx      四档反馈
  chen-teacher-panel.tsx    ★ 追问面板 + 核对档案录入（占位回答在此）
  continue-review.tsx       继续核对（指向下一个未核对年份）
  reviewed-years.tsx        我核对过的年份（按年份归并、六维度状态）
  life-archive.tsx          我的人生档案（按年份连成一条线 + 复制全文）
lib/
  types.ts                  ★ 全部数据模型（接口定义）
  mock-data.ts              ★ 模拟数据引擎（曲线、年度卡片、大限、阶段）（1280 行）
  storage.ts                ★ localStorage 读写（唯一持久化入口）
  notes.ts                  反馈 → 追问文案的生成规则
scripts/                    开发期验证脚本（见 §10，可随时删除）
```

`★` = 接手后最先要读的五个文件。

---

## 4. 数据模型（`lib/types.ts`）

```ts
type FeedbackType = '非常符合' | '部分符合' | '没有印象' | '完全不符合';

interface BirthInfo {
  name?: string;
  gender: '男' | '女' | '其他';
  birthDate: string;   // 'YYYY-MM-DD'
  birthTime: string;   // 例如 '午时 11:00-13:00'，也允许 '不确定'
  birthPlace: string;  // 自由文本
}

type DimensionKey = 'overall' | 'career' | 'wealth' | 'marriage' | 'parents' | 'health';
type Trend = '上升' | '下降' | '震荡' | '转折';
type ActionTip = '推进' | '准备' | '保守' | '重点处理';
type Consistency = '一致' | '单信号' | '冲突';

interface LifeLinePoint {
  age: number; year: number;
  overall: number; career: number; wealth: number;
  marriage: number; parents: number; health: number;  // 内部 5-97，页面只显示档位
  phase: string; trend: Trend;
}

interface YearCardData {
  year: number; age: number;
  daxian: string;          // '32-41 岁 · 夫妻宫大限'
  phase: string;           // '责任扩大期'
  trend: Trend;
  mainJudgment: string;    // 一句主判断（现实语言）
  events: { text: string; priority: number }[];  // 3-6 条，按主次
  ziweiSignal: string;     // 紫微信号
  baziSignal: string;      // 八字信号
  consistency: Consistency;
  actionTip: ActionTip;
}

interface FeedbackRecord { year; dimension; feedback: FeedbackType; updatedAt: number }
type FeedbackMap = Record<string, FeedbackRecord>;   // key: `${year}:${dimension}`

interface YearNote {                                   // ★ 产品核心资产
  year: number; dimension: DimensionKey;
  text: string;          // 用户亲笔写的"这一年实际发生了什么"
  skipped: boolean;      // 用户明确选"先不写"
  updatedAt: number;
}
type YearNoteMap = Record<string, YearNote>;
```

### 四个必须遵守的产品硬约束

1. **页面不得出现具体分数。** 内部 `number` 只用于决定曲线高低与四档标签
   （`levelOf()`：`≥72 高位 / ≥58 中上 / ≥42 中位 / 其余 低位`）。
   需求明确禁止"78 分"这类伪精确表达。
2. **曲线必须是连续的年龄轴。** 横轴年龄 / 公历年份一一对应，
   同一年在不同维度切换时横轴位置**不得变化**。
3. **命理依据默认折叠。** 主判断与可验证事件优先展示，依据放在 `<details>` 里。
4. **可验证事件必须能"想起来"**：具体、可核对，不写"会有贵人相助"这类无法证伪的话。

---

## 5. 数据流与存储

### 5.1 为什么用客户端组件

出生信息与反馈都存在 `localStorage`，服务端读不到。因此：

- `app/dashboard/page.tsx` 只做 metadata + 渲染 `<DashboardClient />`。
- `DashboardClient` 挂载后读一次本地数据；**没有出生信息就 `router.replace('/?need=birth')`**。
- 读取期间显示"正在读取你填写的出生信息…"（这是服务端预渲染的 HTML 内容）。
- `DashboardBody` 用 `key={birthDate + birthTime}` 挂载，换人填写时全部状态自动重置。

> 注意：`useEffect` 里同步 `setState` 会被 `react-hooks/set-state-in-effect` 报错，
> 现有代码在该处用了一行 `eslint-disable-next-line` 并附了原因。改动时请保留注释。

### 5.2 localStorage 合约（`lib/storage.ts`）

| key | 内容 | 写入时机 |
|---|---|---|
| `lifeline.birth` | `BirthInfo` | 首页表单提交 |
| `lifeline.feedback` | `FeedbackMap` | 点四档反馈按钮 |
| `lifeline.notes` | `YearNoteMap` | 点「存进我的核对档案」或「这一年先不写」 |
| `lifeline.draft` | `string` | 追问输入框每次变化（自动保留草稿） |

所有 `load*` 都做了 try/catch 与结构校验，损坏数据返回空值而不是抛错。
`clearFeedback()` 会同时清掉 `feedback` 与 `notes`（顶栏「重新填写」会调用它 + `clearBirth()`）。

**迁移到云端时**：只需替换 `lib/storage.ts` 的实现（改成读写接口）。
其余组件全部通过这 10 个函数访问数据（4 组 × load/save，外加 2 个 clear），不需要改动 UI。

### 5.3 仪表盘状态机（`components/dashboard-client.tsx`）

```
birth            BirthInfo（由外层传入，稳定不变）
dimension        DimensionKey        当前维度
selectedYear     number | null       当前年份
feedbackMap      FeedbackMap         反馈（初始化时读本地）
notes            YearNoteMap         核对档案
panelOpen        boolean             追问面板开关
suggestion       string | undefined  自动带出的追问问题
draft            string              追问草稿
```

派生数据（`useMemo` / 直接计算）：

- `points` = `buildLifeLine(birth)`：1-88 岁完整曲线（88 个点）
- `stage` = `buildStageCard(points, birth)`：当前阶段卡
- `index` / `point`：由 `selectedYear` 定位到曲线上的点
- `reviewedYears`：已核对年份去重集合
- `nextYear` = `nextUnreviewedYear(stage.year - 1, MIN_AGE, reviewedYears)`
- `reviewedRows`：按年份归并的核对记录（给 `ReviewedYears`）
- `archiveEntries`：有原文的条目按年份排序（给 `LifeArchive`）

### 5.4 四条关键行为（改动时别破坏）

| 行为 | 实现位置 | 规则 |
|---|---|---|
| 打反馈后自动追问 | `handleFeedback` | **仅在该年该维度第一次给反馈时**弹出面板并带出 `suggestion`；改档位不重复弹 |
| 追问问题随档位变化 | `lib/notes.ts` → `buildSuggestion` | 四种档位各有两套话术，按 `(year + feedback.length) % 2` 稳定选择；档位与趋势共同决定用词 |
| 继续核对从去年开始 | `nextUnreviewedYear(stage.year - 1, ...)` | 今年还没过完，不下结论。年度卡片把今年单独标为「当前位置」 |
| 跳转后滚动到卡片 | `yearCardRef` + `lastScrolledYear` | 只在年份**真正变化**时滚动，首屏不滚；容器有 `scroll-mt-20` 避开吸顶栏 |

---

## 6. 模拟数据引擎（`lib/mock-data.ts`）

**这是接手后第一个要动、也是唯一需要整体替换的文件。**

### 6.1 三条设计原则

1. **确定性**：同一个人（同一份 `BirthInfo`）每次打开、每次刷新，曲线与文案完全一致。
   实现方式是 FNV-1a 哈希 + 稳定伪随机（`hash/rng/rint/pickN/pickOne`），
   种子由 `birthDate|birthTime|gender` 与年份派生。
   **禁止**引入 `Math.random()` 或 `Date.now()` 参与内容生成。
2. **曲线不加逐年噪声**：`scoreAt()` 只由「长期形状 `SHAPE` + 双正弦个人节律 `WAVE` + 个人整体偏移」
   组成。早期版本加了逐点随机抖动，曲线长满毛刺、不像人生轨迹，已移除。
3. **文案按人生阶段分池**：判断文案分 `early(1-18) / young(19-32) / adult(33+)` 三套
   （`JUDGMENT_POOL_EARLY/YOUNG/ADULT`），事件池按"维度 × 人生阶段带"分（`EVENT_POOL`）。
   这是为了避免童年年份出现"职业决定影响后面十年"这类不合年龄的话——
   **新增文案时必须遵守这个分层**。

### 6.2 对外 API

```ts
CURRENT_YEAR: number                       // new Date().getFullYear()
MIN_AGE = 1; MAX_AGE = 88                  // 曲线覆盖范围
BANDS: Band[]                              // 10 个人生阶段带
bandOf(age) / phaseOf(age)                 // 年龄 → 阶段带 / 阶段名
daxianOf(age): Daxian                      // 年龄 → 大限（2 岁起运，10 年一换，宫位顺序见 PALACES）
buildLifeLine(birth): LifeLinePoint[]      // 生成 88 个点
buildStageCard(points, birth): StageCard   // 当前阶段卡
buildYearCard(points, index, dimension, birth): YearCardData
levelOf(value): Level                      // 数值 → 高位/中上/中位/低位
nextUnreviewedYear(currentYear, minAge, reviewedYears): number | undefined
```

### 6.3 大限与信号规则（当前为演示规则，接真排盘时会整体替换）

- **大限**：2 岁起运，每 10 年一换；宫位顺序硬编码在 `PALACES`
  （父母宫 → 命宫 → 兄弟宫 → 夫妻宫 → 子女宫 → … 与常见安宫顺序相近）。
  **注意**：真实紫微大限的起运岁数与顺逆排序需按性别与生年干支计算，当前是简化版。
- **趋势判定** `trendOf(prev, cur, next)`：相邻年差值 > 2.5 视为方向变化；
  方向不一致 → `震荡`；平稳但有 ≥2 分落差的主峰/主谷 → `转折`。
- **一致性判定**：紫微与八字各自带 `tone`（`good` / `mixed` / `bad`）。
  两者相同 → `一致`；任一为 `mixed` → `单信号`；一好一坏 → `冲突`。
  **文案与判定是绑定的**：改文案必须同步改 `tone`，否则会出现"紫微说大吉、八字说大凶，
  却标注为一致"的矛盾。

### 6.4 各维度曲线的形状设定

`SHAPE` 定义每个维度的关键年龄锚点值（0-100），锚点之间用 `smoothstep` 平滑插值；
`WAVE` 定义每人不同的节律（主周期 + 次周期双正弦）。当前设定：

| 维度 | 走势意图 |
|---|---|
| overall | 儿童期低、40 岁前后见顶、之后缓降 |
| career | 25 岁起爬升、48 岁前后见顶、60 岁后明显回落 |
| wealth | 33 岁后稳步积累、50 岁见顶、65 岁后回落 |
| marriage | 20 岁后快速上升、40 岁前后小低谷、晚年回升 |
| parents | 幼年高位（被照顾）、中年低位（父母老去）、晚年下行 |
| health | 随年龄缓慢下行，50 岁前后最紧张 |

调整这些数值即可改变产品"叙事"，**改完必须跑 §10 的 `smoke.ts` 复核**。

---

## 7. 关键模块说明

### 7.1 `chen-teacher-panel.tsx`（追问面板）

- Props 见文件顶部 `interface Props`，共 12 个；面板**不是**受控弹窗，
  `open === false` 时直接 `return null`。
- 面板内三种交互：发消息（占位回答）、保存核对档案、跳过（写 `skipped=true`）。
- `savedNote` 通过 `useState(savedNote ?? '')` 初始化一次；
  外层用 `key={`${year}:${dimension}`}` 强制换年时重新挂载，避免残留上一年内容。
  **改动时不要去掉这个 key。**
- `placeholderAnswer(year, feedback)` 是**接 AI 的唯一替换点**（见 §8.2）。

### 7.2 `life-archive.tsx` / `reviewed-years.tsx`

- `LifeArchive`：`entries` 为空时整块不渲染（外层 `dashboard-client` 已判断）；
  条目 ≥3 条时默认展开。`buildPlainText()` 生成"复制全文"的文本格式，
  改格式就改这一个函数。
- `ReviewedYears`：按年份分组，每年固定渲染 6 个维度槽位：
  已核对 → 彩色按钮（`非常符合/部分符合` 绿、`没有印象` 灰、`完全不符合` 红；带 ✎ 表示有原文）；
  未核对 → 虚线"待核对"。点彩色按钮回调 `onPick(year, dimension)`。

### 7.3 `life-curve.tsx`（纯 SVG 曲线）

为什么不用图表库：需要「过去实线 + 未来虚线」「当前年龄醒目竖线」「88 个可点击年份节点
且按位置分配命中区域」「横轴双行标签（年龄 + 年份）」这些非常规组合，
手写 SVG 比配置图表库更短、更可控，也不引入额外依赖。

几何参数（改布局看这里）：

```ts
const W = 1136, H = 420;
const PAD = { left: 56, right: 36, top: 40, bottom: 76 };
// 数值 5-97 映射到纵向；四档参考线在 72 / 58 / 42 / 22
```

- 平滑曲线：Catmull-Rom 转三次贝塞尔（`smoothPath`），并把控制点限制在画布内防溢出。
- 命中区域：每个年份一个 `r = max(3.4, 绘图宽/年数/2.1)` 的透明圆，
  带 `role="button"` 与键盘 `Enter/Space` 支持；`.node` 默认透明、hover 时显现（见 `globals.css`）。
- 移动端：外层 `overflow-x-auto`，`useEffect` 里按比例把容器滚到「现在」居中。

### 7.4 样式与设计令牌（`app/globals.css`）

Tailwind v4 的 CSS-first 写法：`:root` 定义变量，`@theme inline` 映射成工具类，
然后就能用 `bg-surface` / `text-ink-2` / `border-line` / `bg-accent-soft` 等。

| 令牌 | 用途 |
|---|---|
| `paper` `surface` `line` `line-strong` | 底色 / 卡片 / 边框 |
| `ink` `ink-2` `ink-3` | 三级文字 |
| `accent` `accent-soft` | 主色（暖橙）与其浅底 |

**只做浅色主题**（没有 `dark:` 分支），SVG 内部颜色是字面量（`#a8531f` 等，定义在
`life-curve.tsx` 顶部常量里）。若要做深色模式，需要同时改这两处。

---

## 8. 如何接入真实能力（两个集成点）

### 8.1 接真实排盘（替换模拟数据）

页面层与数据层已经分离，**UI 不需要改**：

1. 新建 `lib/chart.ts`，导出与 §6.2 同名的函数。
2. 把 `dashboard-client.tsx` 里 `from '@/lib/mock-data'` 的导入改成 `'@/lib/chart'`。
3. 排盘计算（紫微安星、八字排盘）**建议放服务端**：新建 `app/api/chart/route.ts`，
   客户端 `POST { birth }` 拿到结果；`dashboard-client` 里把
   `const points = buildLifeLine(birth)` 换成异步获取 + `useState` 缓存。
   这样可以把排盘库（依赖体积大、常含农历/节气数据）排除出客户端包。
4. `YearCardData` 里 `ziweiSignal / baziSignal / consistency` 三个字段由真实信号生成；
   注意 §6.3 提到的 tone 与一致性绑定关系。

> 数据契约建议保持不变：**只要 `LifeLinePoint` 与 `YearCardData` 的字段不变，
> 9 个组件全部不用动。** 如果真实排盘需要额外字段（如星曜列表、十神），
> 请以可选字段追加，不要改现有字段语义。

### 8.2 接真实 AI（替换占位回答）

1. 新建 `app/api/ask/route.ts`：接收 `{ year, age, dimension, daxian, phase, trend,
   mainJudgment, events, feedback, note, history, question }`，
   在**服务端**拼 system prompt 并调用模型，`OPENAI_API_KEY` 之类的密钥只存在于服务端。
2. `chen-teacher-panel.tsx` 的 `send()` 里，把 `placeholderAnswer(...)` 换成
   `fetch('/api/ask', ...)`；建议用流式响应（`ReadableStream`）+ 逐字追加到 `messages`。
3. **上下文已经备齐**：面板顶部 `history` 是最近历史反馈、`savedNote` 是用户亲笔经历、
   `card` 是当年全部判断要素——这些就是 prompt 的素材，不需要再让用户重复描述背景，
   这正是产品主张的"带着年份和反馈去追问"。
4. 接完 AI 后建议删除 `placeholderAnswer` 里的"（示例回答…）"前缀。

### 8.3 数据持久化

替换 `lib/storage.ts` 的 10 个函数即可（`loadBirth`/`saveBirth`/`clearBirth`、
`loadFeedback`/`saveFeedback`/`clearFeedback`、`loadNotes`/`saveNote`、
`loadDraft`/`saveDraft`）。
建议提供一个"导出我的档案"接口（`LifeArchive` 的 `buildPlainText()` 已经是现成的文本格式），
因为 `lifeline.notes` 是用户不可再生的资产，浏览器清缓存就会丢。

---

## 8.5 后端排盘层的实现口径（重要，动手前必读）

> 这一节回答一个必须先谈清的问题：**后端算出来的东西，我们能承诺"准"吗？**
> 结论：**命盘能算准，判断不能承诺准**。这两件事必须分开实现、分开表述。

### 8.5.1 把「准」拆成两层

| | 内容 | 性质 | 能否保证 |
|---|---|---|---|
| **A 层** | 从出生信息算命盘（安星、四化、大限、流年） | **确定性算法** | **能。** 给定生日+时辰+性别，结果唯一 |
| **B 层** | 从命盘推出「2028 年事业会怎样」 | **解释** | **不能承诺**，但可以测量命中率 |

**工程含义：A 层做完就冻结，B 层可以反复改。** 两层之间用一份"命盘数据结构"隔开
（现有 `LifeLinePoint` / `YearCardData` 就是 B 层的输出格式，**不需要重构**）。

### 8.5.2 A 层：不要从零写，用已验证的实现

| 方案 | 现状（查证日期：2026-09） |
|---|---|
| **iztro**（紫微，TS/JS，MIT） | v2.6.1，npm 上周下载 21,667；**紫微部分已验证正确**（见下方「实测结论」） |
| **lunar-typescript**（八字/农历） | v1.8.6，无第三方依赖；**八字用它，不要用 iztro 自带的** |
| **py-iztro / x-iztro** | iztro 的 Python 版本，用法一致 —— **可用于交叉验证** |
| **JavaScript-For-Paipan** | 农历/干支/四柱/节气互转，支持 -1000～3000 年，**支持真太阳时** |
| **lunar-javascript** | 历法基础库，npm 上周下载 36,438 |

#### ⚠️ 实测结论：iztro 自带的八字**月柱是错的**（必须知道）

用同一份出生信息（1993-06-18 午时）对比两个库：

| | 年柱 | 月柱 | 日柱 | 时柱 |
|---|---|---|---|---|
| **lunar-typescript** | 癸酉 | **戊午** ✅ | 庚午 | 壬午 |
| **iztro**（`chart.chineseDate`） | 癸酉 | **丁巳** ❌ | 庚午 | 壬午 |

**独立验算依据「五虎遁」**：戊癸之年，寅月为甲寅，顺推至午月应为**戊午**。
iztro 按**农历月**（四月 → 巳月）换月，而正确做法是**按节气换月**（芒种后 → 午月）。

**同时验证了 iztro 的紫微部分没有问题**（这是最关键的）：

用 13 个时辰逐一对比 iztro 与 lunar-typescript 的四柱，
**时柱 13/13 完全一致**（差值 0 位），说明：
- iztro 的 `timeIndex` 约定正确（6 = 午时 = 11:00-13:00）
- 它的时辰 → 命宫换算没有错位
- **紫微盘可以放心用**

另有一处**流派差异**（不是错误）：晚子时（23:00-24:00），
iztro 会把日柱推进到次日，lunar-typescript 保持当日。属于不同流派做法，需产品选定。

**工程结论：紫微用 iztro，八字用 lunar-typescript。
不要用 iztro 的 `chineseDate` 字段——它会静默给出错误月柱。**

### 8.5.2b 真太阳时（已实现，见 `lib/solar-time.ts`）

时辰决定命宫，所以必须做。实现要点与三个实测教训：

**公式**：`真太阳时 = 标准时 + 经度差修正 + 均时差`

| 项 | 说明 | 幅度 |
|---|---|---|
| 经度差 | 出生地经度与所属时区标准经线之差，每度 4 分钟 | 乌鲁木齐约 **−130 分** |
| 均时差 | 地球轨道椭圆 + 黄赤交角，用通用近似式 | 一年内 **−14 ~ +16 分** |
| 夏令时 | **不要自己维护日期表**，用系统 IANA 时区库 | ±60 分 |

**夏令时用系统时区库（`Intl.DateTimeFormat` 的 `shortOffset`）**，
这样中国 1986–1991、美国历年规则、各国历史变更全都自动正确，**海外版直接受益**。

**三个踩过的坑（都在测试里抓出来了）**：

1. **夏令时的经度修正算错**：上海应 +5.9 分，却算出 −54.1 分。
   原因是用夏令时偏移（+9）去算标准经线，多算 15°。
   **标准经线必须由「法定标准时区」决定**（先减去夏令时的 60 分钟）。
2. **缓存键漏了出生地**：导致换城市返回同一张盘。
   真太阳时依赖出生地，缓存键必须包含它。
3. **只靠四化无法体现时辰差异**：四化只由流年天干决定，与时辰无关，
   所以换个出生地曲线不变，用户会以为校正没生效。
   **必须把星曜庙旺利陷、以及四化是否落到当事宫计入**，曲线才会随命盘变化。

### 8.5.2c 事件列表也要由排盘生成

年度卡片里的「可验证事件」不能从文案池随机抽，否则会出现
「依据是真的、事件是假的」这种自相矛盾。

**做法（`lib/events.ts`）**：按四化落宫生成事件——

```
四化落在某宫 → 该宫对应生活领域被引动
  命宫 → 整体 / 财帛 → 财富 / 官禄 → 事业
  夫妻 → 婚姻 / 父母 → 长辈 / 疾厄 → 健康
```

**相关性优先（这条是硬规则）**：每个维度的事件必须优先来自它自己的宫位；
只有本宫不够时才用**同类领域**的宫位补。曾经的 bug：财富维度里出现
「和上一代的沟通变顺」，因为父母宫权重高就被优先选中了，
但那条和财富毫无关系，**用户会立刻觉得内容在乱讲**。

**两套文案口径**（产品决定用哪套，界面可切换）：
- `soft` 温和提醒 —— 符合行业习惯，容易被接受
- `direct` 直接事件 —— 更具体，但命中时冲击力也更大

### 8.5.3 A 层四个必须处理的坑（不处理就是算错，不是算不准）

1. **时辰是最大误差源。** 紫微用时辰起命宫，命宫错则十二宫全错。而用户常填"中午前后"。
   → **已在填表阶段做实时提醒**（跨时辰边界 / 离边界 15 分钟内）。
2. **真太阳时（主导人已确认：必须做）。** 详见 §8.5.7。
3. **夏令时与历史时区。** 用系统时区库自动处理。
4. **节气交界与子时归属。** 八字换月按节气（非农历初一）；
   子时（23:00–01:00）算前一天还是当天，流派有分歧（**待产品选定**）。

### 8.5.4 B 层：不能承诺"准"，但有明确的替代口径

**先说清一个事实：同一张命盘，不同流派会得出不同结论**，这不是水平问题：

- 三合派 / 飞星派 / 四化派的解读路径不同；
- 典型分歧：**庚年干四化怎么排，业内长期争论**——而四化是判断的核心依据之一。

**因此产品口径只能说这三件（都能做到）：**

| 承诺 | 实现方式 |
|---|---|
| **算法一致** | 确定性实现 + 双库交叉验证 |
| **口径公开** | 在设置或折叠说明里标出：采用哪一派、真太阳时与否、子时归属、大限规则 |
| **判断可核对** | 文案规范：写"可核对的事件"，不写"吉凶"（现有原型已在执行） |

**禁止在产品文案里出现"我们算得很准"这类表述**——用户会拿去和某位老师傅比，
而对方用的是另一套规则，这个比较对我们没有意义。

### 8.5.5 「准不准」是可以测出来的（这是产品的长期资产）

现有原型已经在收集测量所需的数据（`lifeline.feedback` + `lifeline.notes`）。后端应据此建立统计：

```
某年命中率   = (非常符合 + 部分符合) ÷ 有效反馈数
分维度命中率 = 事业 / 财富 / 婚姻家庭 / 父母支持 / 健康风险 分别统计
分信号强度    = 紫微与八字「一致」的年份 vs 「冲突」的年份，哪类更准
分事件类型    = 换工作 / 搬家 / 生育 / 健康 等哪类事件最容易被用户确认
```

**实现要求（请从第一天就做）：**

1. 每条判断都记录**来源规则**（哪颗星、哪条四化、哪条流年叠加）——否则无法归因；
2. 把用户反馈与亲笔经历按规则聚合，定期出"哪些规则真的有用"的报告；
3. **伪精确数字禁止出现在结论侧**（原始需求明确要求）。精确的是算法，不是结论。

### 8.5.6 落地方案（四条）

1. **两层分离**：`/api/chart` 出命盘数据（冻结）→ 规则引擎出年度卡片（可迭代）。
2. **时辰不确定就明说**：让用户选"确定 / 大概 / 不知道"；不确定时**把可能的时辰都算一遍**，
   对比出"所有时辰都成立的结论"（可以放心讲）与"随时辰变化的结论"（提示用户确认）。
   业内称「生时校正」，做不完整校正也应至少**标注不确定度**。
3. **口径做成配置**：是否真太阳时、子时归属、庚干四化采用哪家、大限几年一换 → 全部配置化，
   不要写死在代码里。这样才能对比不同口径下的命中率。
4. **命中率统计**：见 §8.5.5，这是"AI 命理"与"网页算命"的分界线。

### 8.5.7 真太阳时：必须做（主导人已确认）

**结论：真太阳时进入必做范围。** 因为它确实会改变命盘——时辰决定命宫，
而"北京时间 12:00"不等于"出生地太阳在正上方"；经度差 + 均时差偏差可达 ±30 分钟以上，
卡在时辰边界时命盘会整体换一个。

实现要素：

| 要素 | 做法 |
|---|---|
| 经度 | 出生地 → 经纬度。国内用行政区划表；**海外必须支持任意城市输入并定位到经纬度** |
| 均时差 | **不要手写公式**，用现成实现：npm `solar-time`（本地视太阳时）、`solar-calculator`（太阳位置方程）；参考 [apparent_solar_time](https://github.com/ZhengjunHUO/apparent_solar_time) |
| 时区 | **用"出生地当时的时区"**，不是北京时间。海外用户出生在本地时区，国内用户用东八区 |
| 夏令时 | 国内 1986–1991 年实行过；海外各国规则不同（美国 1918 年起等）。需一张历史规则表 |
| 验证 | 与公开案例、在线排盘工具交叉核对；**重点测卡在时辰交界的样本** |

**对前端表单的影响（海外版尤其）：** 出生城市从"可选文本"升级为**必填且必须能定位到经纬度**。
海外版流程变为「本地出生时间 + 出生城市 → 换算当地真太阳时 → 排盘」，比国内版多一项必填。

### 8.5.8 时辰不确定：不做引导流程，但要标出来（主导人已确认）

**已确认不做**：不枚举 12 个时辰逐个对比、不做生时校正、不做时辰推断。
（12 次排盘 + 对比界面 + 长流程，投入产出不划算。）

**但必须解决它带来的数据问题：**

> 时辰填错 → 命盘就是错的 → 用户对着错的年份给反馈 →
> **收回来的"命中率"是噪音而非数据** → §8.5.5 的验证机制失效。

即：**不处理时辰不确定，损失的不是那一个用户，而是我们唯一的验证手段。**

**采用的低成本方案（三个字段，不加流程）：**

1. 出生信息表单增加一项「时辰是否确定？」（`确定 / 大概 / 不知道`），
   建议在 `BirthInfo` 上追加可选字段 `birthTimeConfidence?: 'exact' | 'approx' | 'unknown'`（**追加字段，不改现有字段语义**）。
2. 时辰为 `approx / unknown` 时，年度卡片打角标：「此年判断随时辰变化」。
3. 设置里允许**随时修改出生时辰，改完自动重算全部年份**（用户的档案不丢，只是重算）。

将来若要做完整「生时校正」，应作为独立的深度服务，不要塞进入门流程。

---

## 9. 已知限制与待办清单

### 已知限制

| 项 | 说明 |
|---|---|
| `npm run dev` 不可用 | 见 §1，环境问题，与代码无关 |
| 数据只在本地 | 换浏览器/清缓存即丢失；无账号、无云同步 |
| 大限与信号是演示规则 | 起运岁数、宫位顺序、十神判断均未按真实算法实现 |
| 只有浅色主题 | 见 §7.4 |
| 无自动化测试框架 | 现有的是浏览器脚本（§10），没有 Jest/Vitest 单测 |
| 无障碍做了基础项 | 键盘可操作曲线节点、`aria-*` 与 `role` 已加；未做完整屏幕阅读器验证 |

### 建议的下一步（按优先级）

1. **接真实排盘**（§8.1）——决定产品是否成立。
2. **接 AI 追问**（§8.2）——决定用户是否愿意回来。
3. **数据上云 + 导出**（§8.3）——决定用户敢不敢认真写。
4. 文案打磨：所有文案目前由 AI 起草，**需要业务方（懂命理与用户的人）逐句过一遍**。
5. 引入 Vitest，先给 `lib/mock-data.ts` 与 `lib/notes.ts` 加纯函数单测
   （这两个文件无 React 依赖，测试成本最低）。

---

## 10. 验证脚本（开发期工具，可随时删除）

### 10.1 命理计算的测试（**最重要，改排盘代码后必跑**）

用 `tsx` 运行（`tsx` 是开发依赖，不进生产包）：

```bash
npx tsx scripts/test-solar-time.ts      # 真太阳时：39 项（均时差、时区、夏令时、经度、边界）
npx tsx scripts/test-bazi.ts            # 八字：32 项（11 条十神规则逐条验算、月柱对五虎遁、大运连续性）
npx tsx scripts/test-events.ts          # 四化落宫 → 事件生成（相关性、两种口气、确定性）
npx tsx scripts/test-signals.ts         # 紫微+八字信号合并、一致性判定分布
npx tsx scripts/test-chart.ts           # 排盘输出（十二宫、大限、四化）
npx tsx scripts/test-driven-curve.ts    # 曲线是否真的随排盘变化
npx tsx scripts/test-qr-scannable.ts    # 二维码能否真的被扫出来（还原成像素后解码验证）
```

**这些测试全部是拿"已知正确的数值"做对照**，不是"看起来对"：
均时差对照天文常识值（2 月约 −14 分、11 月约 +16 分）；
时区对照 IANA 数据库里的中国夏令时真实起止（1986-05-04 ~ 09-14）；
十神对照阴阳生克规则逐条验算；月柱对照五虎遁口诀。

### 10.2 数据与界面测试

需要先启动服务（`node deploy/server.js 8080` 或 `npm start`）。

```bash
# 数据质量：曲线范围、趋势分布、事件条数、文案缺失、确定性、年龄错配
npx tsx scripts/smoke.ts

# 浏览器端（无头 Chrome，截图到 .shots/）
$env:BASE_URL="http://127.0.0.1:8080"
node scripts/verify-ui.mjs            # 主路径（填表→曲线→年度卡片→反馈→追问）
node scripts/verify-review-loop.mjs   # 继续核对 → 写经历 → 档案
node scripts/verify-grouping.mjs      # 按年份归并、点维度跳回
node scripts/verify-chart.mjs         # 排盘在界面上的显示
node scripts/verify-driven.mjs        # 曲线由排盘驱动、两体系方向、口气切换
node scripts/verify-round.mjs         # 圆形屏遮罩与遮挡检测
node scripts/verify-nav.mjs           # 每个页面是否都有回主页的入口
node scripts/verify-qr-changelog.mjs  # 设备扫码屏 + 修改日志页
node scripts/verify-ambient.mjs       # 480×480 模拟器五屏
node scripts/check-mobile.mjs         # 390px 手机尺寸布局
```

`smoke.ts` 的输出里这几项**必须为 0 / 全 true**：

```
事件条数越界的组合数（应为 0）: 0
文案缺失的组合数（应为 0）: 0
两次生成结果一致（应为 true）: true
阶段带缺口数（应为 0）: 0
童年年份出现成人用词的组合数（应为 0）: 0
```

浏览器脚本依赖本机 Chrome 路径（`scripts/*.mjs` 顶部的 `CHROME` 常量），
换机器需要改这一行；也可以换成 Playwright，脚本里的 CDP 调用都是标准协议。

> **为什么用 `tsx` 而不是 `node --experimental-strip-types`**：
> Node 直接跑 TS 时要求 import 带 `.ts` 扩展名，而项目代码（给 Next 打包器用的）不能带。
> `tsx` 解决了这个冲突，且只作为开发依赖，不影响生产构建。

---

## 11. 常见坑

1. **改文案忘了改 tone** → 出现"紫微凶、八字吉却标一致"。见 §6.3。
2. **给 `scoreAt` 加随机抖动** → 曲线立刻长毛刺。见 §6.1。刻意不加逐点噪声。
3. **去掉面板的 `key={year:dimension}`** → 换年时面板残留上一年的输入内容。
4. **在 `useEffect` 里直接 `setState`** → ESLint 新规则报错；本项目只在一处允许（附注释）。
5. **用 PowerShell 的 `Set-Content` 批量改这些文件** → 会按 UTF-8 重写并可能弄坏中文编码。
   请用编辑器的文本编辑功能（或 `edit`/`write` 类工具）。
6. **`next build` 报 `Cannot find module '../../../app/xxx/page.js'`** →
   `.next/dev/types/validator.ts` 里残留了已删除页面的类型。删掉整个 `.next` 重新构建。
7. **页面点击没反应** → 先确认是不是用了 `npm run dev`（见 §1），而不是业务代码问题。

---

## 12. 需求原文对照（验收口径）

| 需求 | 实现位置 | 状态 |
|---|---|---|
| 首页出生信息表单 + 基本必填校验 | `app/page.tsx` + `components/birth-form.tsx` | ✅ |
| 仪表盘当前阶段卡（年龄/年份/大限/阶段名） | `dashboard-client.tsx` 顶部区块 | ✅ |
| 人生总曲线（年龄+年份双轴、无具体分数、现在竖线、过去实线未来虚线、可点击） | `components/life-curve.tsx` | ✅ |
| 六维度切换，同年份位置不变 | `components/dimension-tabs.tsx` | ✅ |
| 年度卡片（趋势/主判断/3-6 条可验证事件/折叠命理依据/四档反馈/行动提示/问陈老师） | `components/year-card.tsx` | ✅ |
| 问陈老师面板（钉住年份·维度·大限·历史反馈 + 输入框 + 占位回答） | `components/chen-teacher-panel.tsx` | ✅ |
| 反馈存 localStorage | `lib/storage.ts` | ✅ |
| 数据模型定义在 `/lib/types.ts` | `lib/types.ts` | ✅ |
| 不接排盘 / 不接 AI / 无登录付费数据库 | — | ✅（符合阶段目标） |
| 超出原需求的新增：继续核对、我的人生档案、核对档案（用户亲笔经历） | `continue-review.tsx` / `life-archive.tsx` / `reviewed-years.tsx` + `lifeline.notes` | ✅ |

---

## 13. 需要业务方确认的开放问题

1. **大限口径**：起运岁数与顺逆排法按哪一派？当前是简化的"2 岁起运、固定顺排"。
2. **"没有印象"的含义**：应视为"判断不准"还是"这一年太平淡"？当前文案按后者处理，
   并建议标记为"待验证"。这会直接影响反馈回流算法。
3. **反馈回流规则**：`非常符合 / 部分符合 / 没有印象 / 完全不符合` 如何影响后续年份的权重？
   目前只存不参与计算。
4. **核对档案的隐私边界**：用户亲笔经历是否上云、是否用于训练、能否一键删除，需要产品明确。
5. **是否要保留"我核对过的年份"中的"待核对"槽位**：它给用户方向感，
   但也可能造成"还有 30 年没做完"的压力，需真实用户验证。
6. **采用哪一派作为主口径**（三合 / 飞星 / 四化）？是否允许用户在设置里切换？
   —— 直接决定 B 层规则引擎怎么写，见 §8.5.4。
7. **子时（23:00–01:00）算前一天还是当天？**
   —— 直接改变命宫，见 §8.5.3。
8. **庚干四化等有争议规则采用哪一家？**
   —— 同一张盘两个老师两个结果的根源，必须写明。
9. ~~时辰不确定的用户要不要支持"多时辰对比"？~~
   —— **已由主导人决定：不做，改为标注 + 可修改时辰**，见 §8.5.8。
10. ~~真太阳时用不用？~~
    —— **已由主导人决定：必须做**，见 §8.5.7。
