# LifeLine 环境变量示例
#
# 复制成 `.env`（这个文件进仓库，`.env` 不进——仓库是公开的）
#
#   copy .env.example .env      # Windows
#   cp .env.example .env        # macOS / Linux

# DeepSeek API key —— 追问功能要用
#
# ⚠️ 这个 key 只在**服务端**使用：
#    浏览器把问题发给本站的 /api/ask，由 Node 服务带上 key 转发给 DeepSeek。
#    key 永远不会出现在网页代码里，用户按 F12 也看不到。
#
# ⚠️ 绝不要把真 key 写进任何 .ts / .tsx / .mjs 源文件——仓库是公开的。
DS_KEY=

# 追问用的模型。
# 注意：deepseek-flash 是**推理模型**，它有思维链，会先花掉一部分 token 再输出答案。
# 所以 MAX_TOKENS 要给足（实测思维链约 900 token，答案约 300 token）。
DS_MODEL=deepseek-flash
DS_MAX_TOKENS=3000

# 服务端兜底限流（防脚本刷）
# 客户端已有每日 3 次的产品限制，这里是防止有人绕开前端直接打接口。
ASK_PER_IP_DAILY=15
